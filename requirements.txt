httpx>=0.25
python-dotenv>=1.0

# Screenshot fallback (TELEGRAM_SOURCE_ENABLED): channel access and OCR.
# Dropping the fallback drops these, tesseract-ocr and libgl1 from the image.
telethon>=1.34
pytesseract>=0.3.10
opencv-python-headless>=4.8
numpy>=1.26
